extern int putchar(int c);

int[] numbers = [100, 101, 102];

int main() {
    int index = 0;
    while (index < 3) {
        putchar(numbers[index]);
        index = index + 1;
    }

    return 0;
}