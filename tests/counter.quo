extern int putchar(int c);

int numberToChar(int n)
{
    return n + 48;
}

int main()
{
    int counter = 9;
    while (counter >= 0) {
        putchar(numberToChar(counter));
        // Newline
        putchar(10);
        counter = counter - 1;
    }
    
    return 0;
}